# 拍立得周记 (Weekly Polaroid) - App 需求文档

## 1. 产品概述
本产品是一款以“周”为维度的极简视觉灵感周记 App。用户可以按周记录每天通过照片定格的瞬间，系统会自动通过 AI 分析照片并提取核心标签/词汇。产品结合了拍立得的复古物理手感（如卡片滑动、碎纸机删除动画）和现代大语言模型的归纳总结能力，从而帮助用户梳理每一周的生活、灵感与感悟。

### 1.1 核心价值
- **轻量记录**：没有繁重的打字压力，只需贴图，由 AI 生成图像关键词。
- **回顾总结**：通过周维度的 AI 总结，生成每周回顾，辅助完成周播客、碎片想法的聚合。
- **物理隐喻**：通过贴图、滑看照片栈、碎纸机删除等动效，提供真实、带感的物理世界反馈。

## 2. 核心功能及交互需求

### 2.1 日程时间轴与周管理 (Timeline & Week Navigation)
- **按周展现**：默认首页视图显示“本周”的所有天数打卡槽。
  - 列表形态：周一到周五各自独立一个 Slot，周六和周日合并为一个 Slot。
  - 周标题栏显示当前的年份及周期号（如：`2026-W25`）。
- **时间切换**：
  - 点击左右箭头，或在时间轴整体左右滑动，能快速回到上一周、进入下一周。
  - 提供快速“回到今天”的快捷功能按钮。

### 2.2 拍立得相框与日常打卡 (Polaroid Day Slots)
- **上传照片**：点击特定日期的槽位可以唤起相册/相机，用户上传/拍摄相片。
- **AI 自动标签化提取**：照片上传完毕后，系统静默调用 AI 视觉大模型（可配置厂商），提取照片中的主题、氛围、关键词等（称为 Terms），并将其以标签手写体的形式显示在拍立得卡片下方的留白处。
- **卡片栈手势交互**：
  - 若一天有多张相片，会以「相片栈」的形式堆叠在一起。
  - **拖拽滑看 (Swipe to Swap)**：用户按住顶层卡片向左/向右滑动松开，可将顶层卡片轮换到底部，露出下一张照片，并带阻尼回弹动效。
- **查看与缩放**：点击单张拍立得，可进行放大预览 (Zoom)，查看原图与全部标签。
- **碎纸机删除动画**：
  - 当用户在单张图点击左上角的“删除(X)”时，不使用生硬的系统弹窗。
  - 弹出自定义确认提示，一旦确认删除，界面播放“碎纸机粉碎该拍立得”的趣味动画后，彻底移除该记录。
- **标签编辑**：对于 AI 生成的标签，用户支持手动点击“x”删除不想要的标签，支持复制标签，支持手动新增自定义标签。

### 2.3 灵感笔记与 AI 总结板 (Weekly Notes & AI Summarize)
- **便签区域**：屏幕下方有一个可以控制高度的底部面板（Bottom Sheet/Drawer 隐喻），用于记录用户的「每周笔记」。可以随意拖拽改变面板高度。
- **一键 AI 总结 (Auto Summarize)**：
  - 收集用户记录在这一周内所有照片的打卡标签。
  - 一键交给 AI（如 Gemini/Claude）根据提取的标签碎片，生成一篇这周的总结短文/灵感日志，自动追加到下方“每周笔记/灵感板”中。
- **云端防丢同步**：输入便签时，应用自带防抖（1.2s）自动上传同步功能，避免丢失。

### 2.4 图库预览与搜索模式 (Gallery Preview & Search)
- **本周瀑布流预览 (Weekly Preview)**：点击右上角网格按钮，弹出一个本周所有相片的相册画廊（瀑布流模式），方便一览无遗。
- **全局与本周搜索**：
  - 顶部导航提供 Search 栏。
  - 搜索支持匹配内容标签（Terms）。
  - 可以选择搜索范围为“仅本周 (Current Week)” 或者“全部时间 (All Time)”。

### 2.5 高级自定义设置 (Settings & AI Model Configurations)
- **模型定制 (BYOK - Bring Your Own Key)**：允许极客用户或高级会员在设置中配置自定义 AI 模型：
  - 支持引擎切换：Gemini 或 Anthropic。
  - 允许自定义 API Key。
  - 允许配置自定义的 Base URL 层（适配各种反向代理或内部服务）。
  - 指定不同的具体模型名（如 `gemini-3.5-flash` 或 `claude-3-5-sonnet-latest`）。
- **主题设置**：支持亮色/暗色 (Light/Dark mode) 一键切换。

## 3. App 适配及设计建议（移动端改造建议）

由于是从 Web 移植或改造为 Native App，以下为需特殊关注的技术或体验点：

1. **手势与触控响应 (Touch & Gestures)**：
   - 拍立得照片栈的堆叠替换，需要使用更平滑的 Native 弹簧动画（Reanimated / SwiftUI Spring），拖动手势的灵敏度和打断阈值需要为设备端重新调优。
2. **底层存储系统 (Storage & Sync)**：
   - 当前逻辑类似纯远程或实时监听同步，App 层建议增加完善的 **Local-First 离线数据库** (如 SQLite / WatermelonDB)，并使用后台静默同步队列进行远端数据（Firebase/Supabase）交互，保证无网环境也能记录及翻阅相册。
3. **沉浸式相机支持**：
   - 添加相片时，除了相册选择，更要强化直接调用系统相机“咔嚓”随手拍的能力，并提供相应的快门动效。
4. **性能与流优化**：
   - 在网格画廊预览、多张原图照片加载时，需要做图片压缩并妥善利用本地图片缓存框架 (FastImage/SDWebImage 等)。
5. **设备特有反馈 (Haptics)**：
   - 在卡片滑动替换、进入碎纸机粉碎的瞬间、笔记长拉阻尼拉倒极值等地方，加入 Taptic Engine 细腻的震动反馈，强化物理拟真感。
